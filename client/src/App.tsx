import { useState } from 'react';

export default function App() {
  return (
    <div className="container mt-5 text-center">
      <h1 className="fw-bold">TokTickIT</h1>
      <p className="text-muted">React + TypeScript + Vite + Bootstrap Foundation Setup</p>
    </div>
  );
}